# Install and Project Set Up

## Frontend

cd into `canopy-FRONTEND`
`npm i` to install
`npm run dev` to start vite (running at [http://localhost:5173/](http://localhost:5173/))

## Backend

cd into `BACKEND`
`npm i` to install
`npm run start` to start server (watch enabled)
